Some experimental Java code to do analysis of sound files. Draws "wasserfall" diagrams on logarithmic (note-) scale, and reads them back in to synthesize sound.

(Optionally) uses FFTW/jni to speed up the heavy FFT work. 
